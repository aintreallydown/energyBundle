<h2>{{ 'bundle.energy.h2.1' | trans }}</h2>

<ul class="naked">

	{% for label, value in field_choices(form.energy) %}
		<li>
			<label class="flex left middle sm">
				<input 
					class="mr-15"
					type="radio"
					name="{{ field_name(form.energy) }}" 
					{{ field_value(form.energy) == label ? 'checked' : '' }}
					value="{{ label }}"
				>
				<img height="35" src="{{ absolute_url(asset('icons/dpe-' ~ label ~ '.svg')) }}">

				{{dump(value)}}
				{{dump(label)}}
				{{dump(form.energy)}}
				{{dump(form.energy.vars)}}
				{{dump(form.energy.vars.choices)}}
				{{dump(form.energy.vars.choices[0])}}
				{{dump(form.energy.vars.choices[0].data)}}
				{{dump(form.energy.vars.choices[0].data.valueMin)}}
				{{dump(form.energy.vars.choices[0].data.valueMax)}}
				{# {% if value.data.valueMin is not null and value.data.valueMax is not null %}
					{{ value.data.valueMin ~ ('bundle.energy.range_separator' | trans) ~ value.data.valueMax }}
				{% else %}
					{{ value.data.valueMin is not null ? value.data.valueMin : value.data.valueMax }}
				{% endif %} #}
			</label>
		</li>
	{% endfor %}
</ul>

	{# <h2>{{ 'bundle.energy.h2.2' | trans }}</h2> #}

	{# <ul class="naked">
		
		{% for label, value in field_choices(form.emissions) %}
            <li>
				<label class="flex left middle sm">

	                <input 
						class="mr-15"
	                    type="radio"
	                    name="{{ field_name(form.emissions) }}" 
	                    {{ emissions == value ? 'checked' : '' }}
	                    value="{{value}}"
	                >
					<img height="35" src="{{ absolute_url(asset('icons/ges-' ~ value ~ '.svg')) }}">
	                {{ label }}
				</label>
            </li>
        {% endfor %}
	</ul> #}
    